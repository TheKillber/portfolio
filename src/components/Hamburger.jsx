import React from "react";

const Hamburger = ({ className }) => {
   return (
      <div className={className}>
         <span />
         <span />
         <span />
         <span />
      </div>
   );
};

export default Hamburger;
